{
  /* Visit https://firebase.google.com/docs/database/security to learn more about security rules. */
  /*remeber to read up on this and change these soon - but for now starting with completely permisive rules*/
  "rules": {
    ".read": true,
    ".write": true
  }
}
